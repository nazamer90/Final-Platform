import { SpeedInsights } from "@vercel/speed-insights/react";

/**
 * SpeedInsightsComponent
 * Vercel Speed Insights integration component for the EISHRO e-commerce platform
 * Tracks performance metrics and sends data to Vercel's analytics dashboard
 */
export function SpeedInsightsComponent() {
  return <SpeedInsights />;
}
