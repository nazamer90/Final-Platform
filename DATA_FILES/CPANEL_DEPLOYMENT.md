# 📋 دليل النشر الكامل على استضافة CPanel
## EISHRO Platform - ishro.ly

---

## **معلومات الخادم 📌**

```
Server IP: 102.213.180.22
CPanel URL: https://102.213.180.22:2083
Domain: ishro.ly & www.ishro.ly
Hosting Type: Cloud Shared Hosting (Node.js Support)
CPanel User: ghoutni@gmail.com
```

---

## **المرحلة الأولى: تسجيل الدخول إلى CPanel**

### الخطوة 1: الدخول
1. اذهب إلى: `https://102.213.180.22:2083`
2. أدخل بيانات الدخول:
   - **Username:** `ghoutni@gmail.com`
   - **Password:** `@Dm1ns$$2025`
3. عند طلب رمز التحقق (Verification Code):
   - ستستقبل الرمز على بريدك الإلكتروني
   - أدخل الرمز في CPanel

### الخطوة 2: تحديد منفذ Node.js
⚠️ **مهم جداً:**
- المنفذ 2083 خاص بـ CPanel
- اختر منفذاً لـ Node.js من المنافذ المتاحة:
  - **3000** (الموصى به)
  - 8000
  - 8080
  - 8888
  - 9000

---

## **المرحلة الثانية: رفع المشروع عبر File Manager**

### الخطوة 1: فتح File Manager
```
CPanel Dashboard → File Manager → Home Directory
```

### الخطوة 2: إنشاء هيكل المشروع
```
home/
└── ghoutni/
    └── public_html/
        ├── app/  (المشروع الكامل)
        ├── index.html
        └── ...
```

### الخطوة 3: رفع المشروع

**الطريقة الأولى: رفع ZIP**
1. اضغط على "Upload"
2. اختر ملف ZIP للمشروع الكامل
3. بعد الرفع: اضغط كليك يمين → "Extract"

**الطريقة الثانية: النسخ المباشر**
1. استخدم SCP أو FTP
2. اتصل بـ: `102.213.180.22:22` (SSH)
3. المستخدم: `ghoutni`
4. أرفع المشروع

---

## **المرحلة الثالثة: إعداد قاعدة البيانات**

### الخطوة 1: إنشاء Database

```
CPanel → MySQL Databases
```

1. أدخل اسم Database: `ishro_production`
2. اضغط "Create Database"

### الخطوة 2: إنشاء MySQL User

```
CPanel → MySQL Databases → MySQL Users
```

1. **Username:** `ishro_user`
2. **Password:** (اختر كلمة مرور قوية)
3. اضغط "Create User"

### الخطوة 3: إعطاء الصلاحيات

```
CPanel → MySQL Databases → Add User to Database
```

1. اختر الـ User: `ishro_user`
2. اختر الـ Database: `ishro_production`
3. حدد جميع الصلاحيات ✓
4. اضغط "Add"

---

## **المرحلة الرابعة: تحديث ملف .env**

### الخطوة 1: تحديث بيانات قاعدة البيانات

داخل المجلد: `/home/ghoutni/app/`

اعدل ملف `.env.production`:

```env
# Database
DB_HOST=localhost
DB_PORT=3306
DB_USER=ishro_user
DB_PASSWORD=YOUR_PASSWORD_HERE
DB_NAME=ishro_production

# Frontend
FRONTEND_URL=https://www.ishro.ly
VITE_FRONTEND_URL=https://www.ishro.ly

# API
VITE_API_URL=https://www.ishro.ly/api
BACKEND_URL=https://www.ishro.ly

# Security
JWT_SECRET=YOUR_SECURE_SECRET_HERE
CORS_ORIGIN=https://www.ishro.ly
```

### الخطوة 2: نسخ .env للإنتاج

```bash
cp .env.production .env
```

---

## **المرحلة الخامسة: تثبيت المتطلبات وبناء المشروع**

### الخطوة 1: فتح Terminal في CPanel

```
CPanel → Terminal
```

### الخطوة 2: الذهاب لمجلد المشروع

```bash
cd /home/ghoutni/public_html/app
```

### الخطوة 3: تثبيت المتطلبات

```bash
# تثبيت Frontend
npm install

# تثبيت Backend
cd backend
npm install
cd ..
```

### الخطوة 4: بناء Frontend

```bash
npm run build
```

**ملفات الإخراج:** `dist/` folder

---

## **المرحلة السادسة: إعداد Backend (اختر أحد الخيارات)**

### **الخيار الأول: استخدام PM2 (موصى به) ⭐**

#### أ. تثبيت PM2
```bash
npm install -g pm2
```

#### ب. إنشاء ملف Startup

```bash
pm2 start ecosystem.config.js --env production
```

#### ج. حفظ العملية
```bash
pm2 save
pm2 startup
```

---

### **الخيار الثاني: Node.js Selector في CPanel**

1. اذهب إلى: `CPanel → Node.js Selector`
2. اختر الإصدار: **14.0.0 أو أحدث**
3. اختر المجلد: `/home/ghoutni/public_html/app`
4. ملف البدء: `backend/src/index.ts`
5. اضغط "Setup Node.js App"

---

## **المرحلة السابعة: ربط النطاق بـ Backend**

⚠️ **مهم جداً:** بدون هذه الخطوة لن يعمل!

### الخطوة 1: Reverse Proxy

```
CPanel → Domains
```

للنطاق `ishro.ly`:
1. اضغط على النطاق
2. اذهب إلى "Proxy Information"
3. أضف:
   ```
   ProxyPass / http://localhost:3000/
   ProxyPassReverse / http://localhost:3000/
   ```

كرر نفس العملية لـ `www.ishro.ly`

---

## **المرحلة الثامنة: تفعيل SSL/HTTPS**

### الخطوة 1: تثبيت شهادة SSL

```
CPanel → Domains → AutoSSL
```

1. اختر النطاقات: `ishro.ly` و `www.ishro.ly`
2. اضغط "Check & Install"
3. سيتم التثبيت خلال دقائق

### الخطوة 2: إعادة التوجيه الإجباري

```
CPanel → Domains → Force HTTPS Redirect
```

فعّل الخيار: "Force HTTPS Redirect"

---

## **المرحلة التاسعة: اختبار النظام**

### اختبر الروابط التالية:

```
✅ https://www.ishro.ly
✅ https://ishro.ly
✅ https://www.ishro.ly/api/health
✅ https://www.ishro.ly/auth/google/callback
```

### اختبر الوظائف:

- [ ] تسجيل الدخول
- [ ] إنشاء متجر جديد
- [ ] إضافة منتج
- [ ] تحميل صورة
- [ ] تسجيل الدخول عبر Google
- [ ] عملية الدفع

---

## **المرحلة العاشرة: مراقبة الأداء**

### عرض السجلات

```bash
# سجلات PM2
pm2 logs ishro-backend

# إحصائيات النظام
pm2 monit
```

### مراقبة استهلاك الموارد

```
CPanel → System Health
CPanel → Resource Usage
```

---

## **أوامر مهمة للصيانة**

```bash
# إعادة تشغيل Backend
pm2 restart ishro-backend

# إيقاف Backend
pm2 stop ishro-backend

# تشغيل Backend
pm2 start ishro-backend

# حذف البروسيس
pm2 delete ishro-backend

# عرض جميع البروسيسات
pm2 list

# حفظ الحالة
pm2 save

# عرض التفاصيل
pm2 show ishro-backend
```

---

## **استكشاف الأخطاء**

### ❌ الخطأ: "Port Already in Use"
```
الحل: اختر منفذاً مختلفاً في ecosystem.config.js
```

### ❌ الخطأ: "Database Connection Failed"
```
تحقق من:
- بيانات قاعدة البيانات صحيحة
- قاعدة البيانات موجودة
- المستخدم له صلاحيات
```

### ❌ الخطأ: "CORS Error"
```
تأكد من أن CORS_ORIGIN في .env يطابق النطاق:
CORS_ORIGIN=https://www.ishro.ly
```

### ❌ الخطأ: "SSL Certificate Not Found"
```
أعد تشغيل AutoSSL:
CPanel → Domains → AutoSSL → Check & Install
```

### ❌ الخطأ: "404 Not Found on Frontend Routes"
```
تأكد من أن Static Files موجودة في public_html:
- dist/index.html
- dist/css/
- dist/js/
```

---

## **النسخ الاحتياطية 💾**

### نسخ احتياطي يومي من قاعدة البيانات

```bash
# في Terminal
cd /home/ghoutni
mysqldump -u ishro_user -p ishro_production > backup_$(date +%Y%m%d).sql
```

### نسخ احتياطي من المشروع

```bash
# ضغط المشروع
tar -czf app_backup_$(date +%Y%m%d).tar.gz app/

# تحميل عبر File Manager أو FTP
```

---

## **الجدول الزمني للنشر** ⏱️

```
1. تسجيل الدخول لـ CPanel           → 5 دقائق
2. رفع المشروع                    → 10 دقائق
3. إعداد قاعدة البيانات            → 5 دقائق
4. تثبيت المتطلبات                 → 10 دقائق
5. بناء الواجهة الأمامية           → 5 دقائق
6. تشغيل Backend                   → 5 دقائق
7. تفعيل SSL                      → 5 دقائق
8. الاختبار الشامل                 → 10 دقائق

إجمالي الوقت: ~55 دقيقة
```

---

## **قائمة التحقق النهائية ✅**

- [ ] تم تسجيل الدخول لـ CPanel بنجاح
- [ ] تم تحديد منفذ Node.js المتاح
- [ ] تم رفع المشروع الكامل
- [ ] تم إنشاء قاعدة البيانات
- [ ] تم تحديث ملف .env بالبيانات الصحيحة
- [ ] تم تثبيت npm المتطلبات
- [ ] تم بناء الواجهة الأمامية (npm run build)
- [ ] تم تشغيل Backend بنجاح
- [ ] تم تفعيل SSL/HTTPS
- [ ] تم اختبار جميع الروابط الأساسية
- [ ] تم التحقق من الوظائف الرئيسية
- [ ] تم إعداد النسخ الاحتياطية

---

## **المراجع المهمة**

- [CPanel Docs](https://docs.cpanel.net/)
- [PM2 Docs](https://pm2.keymetrics.io/docs)
- [Node.js Docs](https://nodejs.org/docs/)
- [Let's Encrypt SSL](https://letsencrypt.org/)

---

**آخر تحديث:** 2025-12-09  
**الإصدار:** 1.0.0  
**الحالة:** جاهز للنشر ✅
