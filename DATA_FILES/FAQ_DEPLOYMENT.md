# ❓ الأسئلة الشائعة - Fly.io & Vercel Deployment

## 🌐 عن Fly.io

### س1: هل Fly.io مجاني حقاً؟

**ج:** نعم! ✅

**الحدود المجانية:**
- 3 Shared-CPU VMs (ماكينات افتراضية)
- 3GB Persistent Storage
- 160GB Outbound Traffic شهرياً

**مثال حسابي لمشروع صغير-متوسط:**
- 1 VM للـ Backend = مجاني ✅
- 1GB Storage للـ Uploads = مجاني ✅
- 50GB Traffic/شهر = مجاني ✅

**طالما لم تتجاوزي هذه الحدود، لن تُدفع أي مبلغ!**

إذا تجاوزت الحدود، سيُرسل لك تنبيه قبل الخصم.

---

### س2: لماذا يطلبون بطاقة ائتمان إذا كان مجاني؟

**ج:** للتحقق من الهوية فقط (منع الاحتيال)

- ✅ لن يُخصم أي مبلغ في الخطة المجانية
- ✅ تنبيه قبل أي رسوم
- ✅ يمكنك حذف البطاقة بعد إضافتها

**بدائل إذا لم يكن لديك بطاقة:**
- استخدمي **Railway** (يعطي 5$ trial بدون بطاقة)
- أو **Koyeb** (لا يطلب بطاقة)

---

### س3: ما الفرق بين Fly.io و Render؟

| الميزة | Fly.io | Render |
|--------|--------|--------|
| **Free Tier** | ✅ دائم | ✅ محدود |
| **VMs** | 3 مجانية | 1 مجانية |
| **Sleep** | لا ينام | ينام بعد 15 دقيقة عدم نشاط |
| **Regions** | 35 منطقة | محدودة |
| **Performance** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |

**الخلاصة:** Fly.io أفضل! ✅

---

### س4: هل Fly.io يدعم MySQL؟

**ج:** نعم! ✅

Fly.io يدعم الاتصال بأي قاعدة بيانات خارجية:
- ✅ MySQL (CPanel, AWS RDS, etc.)
- ✅ PostgreSQL
- ✅ MongoDB
- ✅ SQLite
- ✅ أي قاعدة بيانات عبر Network

ليس عليك استخدام قواعد بيانات Fly.io - يمكنك استخدام MySQL من CPanel مباشرة.

---

### س5: هل التطبيق سيتوقف إذا لم يُستخدم؟

**ج:** لا! ❌

مع الإعدادات في `fly.toml`:
```toml
auto_stop_machines = false
min_machines_running = 1
```

التطبيق سيعمل **24/7** بدون توقف ✅

**الفرق عن Render Free Tier:**
- Render: ينام بعد 15 دقيقة → Cold Start 30-60 ثانية
- Fly.io: لا ينام → Response فوري دائماً

---

### س6: كيف أعرف إذا تجاوزت الحدود المجانية؟

**ج:** 

**الطريقة 1: Dashboard**
```
https://fly.io/dashboard → Billing
```

سترى:
- Usage هذا الشهر
- Remaining في Free tier

**الطريقة 2: CLI**
```bash
fly dashboard
```

**الطريقة 3: Email Alerts**
- Fly.io يرسل تنبيه عند 80% من الحدود
- ثم تنبيه قبل أي رسوم

---

## 🎨 عن Vercel

### س7: هل Vercel مجاني للاستخدام التجاري؟

**ج:** يعتمد على الخطة:

**Hobby Plan (مجاني):**
- ✅ للمشاريع الشخصية
- ✅ للـ Portfolios
- ⚠️ **ليس للاستخدام التجاري**

**Pro Plan (20$/شهر):**
- ✅ للاستخدام التجاري
- ✅ Custom domains
- ✅ Advanced analytics

**لمشروعك:**
- إذا تجاري → استخدمي Pro Plan
- إذا شخصي/تجريبي → Hobby Plan مقبول

**بديل مجاني تجاري:** Netlify (يسمح بالاستخدام التجاري في Free tier)

---

### س8: هل يمكنني استخدام CPanel للـ Frontend بدلاً من Vercel؟

**ج:** نعم، ممكن! ✅

**المقارنة:**

| الميزة | CPanel Static | Vercel |
|--------|--------------|--------|
| **التكلفة** | 0$ (موجود) | 0$ (Hobby) |
| **CDN** | ❌ لا | ✅ عالمي |
| **Auto-Deploy** | ❌ لا (رفع يدوي) | ✅ من Git |
| **SSL** | حسب CPanel | ✅ تلقائي |
| **السرعة** | حسب السيرفر | ⚡ عالية جداً |

**موصى به:** Vercel (أفضل تجربة)

**لكن:** إذا أردت CPanel:
1. Build محلياً: `npm run build`
2. ارفعي مجلد `dist/` إلى CPanel
3. وجهي النطاق للمجلد

---

### س9: Auto-Deployment - كيف يعمل؟

**ج:**

**Vercel (Frontend):**
```
1. تعدلي الكود محلياً
2. git push origin main
3. Vercel يكتشف التحديث تلقائياً
4. يبني المشروع
5. ينشر الإصدار الجديد
━━━━━━━━━━━━━━━━━━━━
⏱️ الوقت: 1-2 دقيقة
✅ تلقائي 100%
```

**Fly.io (Backend):**
```
1. تعدلي الكود محلياً
2. git push origin main
3. fly deploy (يدوياً)
4. يبني Docker image
5. ينشر الإصدار الجديد
━━━━━━━━━━━━━━━━━━━━
⏱️ الوقت: 3-5 دقائق
⚠️ يحتاج أمر يدوي
```

**يمكن جعل Fly.io تلقائي:**
- استخدمي GitHub Actions
- راجعي: https://fly.io/docs/app-guides/continuous-deployment-with-github-actions/

---

## 🗄️ عن Database

### س10: هل يجب استخدام MySQL من CPanel؟

**ج:** لا، لديك خيارات:

| الخيار | التكلفة | المميزات | العيوب |
|--------|---------|----------|---------|
| **CPanel MySQL** | 0$ (موجود) | سهل، موجود | حسب أداء السيرفر |
| **PlanetScale** | 0$ (5GB) | Serverless MySQL | 5GB حد أقصى |
| **Neon** | 0$ (512MB) | Serverless Postgres | صغير |
| **MongoDB Atlas** | 0$ (512MB) | NoSQL | يحتاج تغيير ORM |
| **Fly.io Postgres** | 0$ (3GB) | مدمج مع Fly | Postgres بدل MySQL |

**موصى به:** ابقي على CPanel MySQL (موجود ومجاني) ✅

---

### س11: Remote MySQL في CPanel - هل آمن؟

**ج:** إذا أُعد بشكل صحيح، نعم ✅

**الطريقة الآمنة:**

**❌ غير آمن:**
```
Access Host: %  (جميع IPs - خطر!)
```

**✅ آمن:**
```
Access Host: 66.241.124.0/24  (Fly.io IPs فقط)
Access Host: 66.241.125.0/24
Access Host: Your_Office_IP
```

**نصائح إضافية:**
1. استخدمي **SSL/TLS** للاتصال بـ MySQL
2. كلمة مرور قوية (16+ أحرف)
3. Backup منتظم
4. راقبي Logs للاتصالات المشبوهة

---

### س12: ماذا لو نفذت مساحة Database؟

**ج:**

**في CPanel:**
1. راجعي خطة الاستضافة - كم المساحة المتاحة؟
2. نظفي البيانات القديمة (old logs, carts)
3. أرشفي البيانات القديمة

**البدائل:**
- ترقية خطة CPanel
- الانتقال لـ PlanetScale (5GB مجاني)
- استخدام Fly.io Postgres (3GB مجاني)

---

## 💻 عن التطوير المحلي

### س13: كيف أختبر محلياً قبل النشر؟

**ج:**

**Frontend:**
```bash
cd /path/to/project
npm run dev
```

سيعمل على: `http://localhost:5173`

**Backend:**
```bash
cd /path/to/project
npm start  # أو: node server.js
```

سيعمل على: `http://localhost:5000`

**Database:**
- استخدمي MySQL محلي (XAMPP, MAMP)
- أو اتصلي بـ CPanel MySQL مباشرة

**.env.local:**
```env
VITE_API_URL=http://localhost:5000/api
DB_HOST=localhost  # أو CPanel host
```

---

### س14: هل يمكنني اختبار Fly.io محلياً؟

**ج:** نعم! ✅

**باستخدام Docker:**

```bash
# بناء Docker image
docker build -t eishro-backend .

# تشغيل محلياً
docker run -p 8080:8080 --env-file .env.fly.production eishro-backend
```

**اختبار:**
```
http://localhost:8080/health
```

**فائدة:** تكتشفي مشاكل Docker قبل النشر

---

## 🔐 عن الأمان

### س15: هل Secrets آمنة في Fly.io و Vercel؟

**ج:** نعم، آمنة جداً ✅

**Fly.io:**
- Secrets مشفرة at rest
- لا تظهر في Logs
- يمكنك فقط عرض الأسماء (ليس القيم)

**Vercel:**
- Environment Variables مشفرة
- لا تظهر في Build logs
- منفصلة لكل Environment (Production, Preview)

**نصيحة:** لا تضعي Secrets في Git أبداً!

---

### س16: ماذا أفعل إذا تسرب Secret؟

**ج:**

**1. غيري الـ Secret فوراً:**

**في Fly.io:**
```bash
fly secrets set JWT_SECRET=new-secret-key
```

**في Vercel:**
- Settings → Environment Variables
- عدلي القيمة → Save
- Redeploy

**2. إذا كان Database Password:**
- غيريه في CPanel أولاً
- ثم حدثي في Fly.io

**3. راقبي Logs:**
```bash
fly logs | grep error
```

---

## 📊 عن الأداء

### س17: كم عدد المستخدمين المتزامنين الذي يدعمه Fly.io المجاني؟

**ج:**

**مع الخطة المجانية:**
- **1 VM Shared-CPU + 256MB RAM:**
  - **~50-100 مستخدم متزامن** (Concurrent)
  - **~1,000-2,000 مستخدم/يوم** (Daily Active)

**إذا احتجت أكثر:**
- زيادة RAM: `fly scale memory 512` (~5$/شهر)
- زيادة VMs: `fly scale count 2` (مجاني إذا ضمن 3 VMs)

**مثال:**
- 2 VMs × 256MB = ~100-200 متزامن
- لا يزال مجاني! ✅

---

### س18: Backend بطيء - ما الحل؟

**ج:**

**الأسباب المحتملة:**

1. **Database بطيء:**
   - أضيفي indexes في MySQL
   - استخدمي Connection pooling
   - نفذي query optimization

2. **RAM غير كافي:**
   ```bash
   fly scale memory 512
   ```

3. **Region بعيد:**
   - إذا معظم مستخدمينك من منطقة أخرى
   - أضيفي region إضافي:
   ```bash
   fly regions add lhr  # London
   ```

4. **Caching غير مفعّل:**
   - فعلي Redis (Upstash مجاني 10MB)
   - أو استخدمي in-memory cache

---

## 🔄 عن التحديثات

### س19: كم مرة يمكنني النشر؟

**ج:**

**Fly.io:** غير محدود ✅
```bash
fly deploy  # كل ما تريدين
```

**Vercel:** 
- **Hobby:** 100 deployments/يوم
- **Pro:** Unlimited

**عملياً:** أكثر من كافي!

---

### س20: كم يستغرق Deploy؟

**ج:**

**Fly.io Backend:**
- **أول مرة:** 3-5 دقائق (بناء Docker image)
- **التحديثات:** 2-3 دقائق (يستخدم cache)

**Vercel Frontend:**
- **أول مرة:** 1-2 دقيقة
- **التحديثات:** 30-60 ثانية (cache ممتاز)

---

## 💸 عن التكاليف

### س21: متى سأحتاج للترقية للخطة المدفوعة؟

**ج:**

**Fly.io - ستحتاجين الترقية إذا:**
- تجاوزت 3 VMs
- تجاوزت 3GB Storage
- تجاوزت 160GB Traffic/شهر

**مثال:**
- Traffic: 5GB/يوم × 30 يوم = 150GB/شهر → مجاني ✅
- Traffic: 10GB/يوم × 30 يوم = 300GB/شهر → 5-10$/شهر

**Vercel - ستحتاجين Pro إذا:**
- استخدام تجاري
- Custom domains > 1
- Team members > 1

---

### س22: كم تكلفة Fly.io المدفوعة؟

**ج:**

**Pay-as-you-go:**

| Resource | السعر |
|----------|-------|
| **Shared CPU VM** | 0.0000008$/s (~2$/شهر) |
| **RAM** (256MB) | مجاني |
| **RAM** (512MB) | ~5$/شهر |
| **Storage** (1GB) | 0.15$/شهر |
| **Bandwidth** (1GB) | 0.02$/GB |

**مثال (مشروع متوسط):**
- 1 VM + 512MB RAM = 5$/شهر
- 5GB Storage = 0.75$/شهر
- 200GB Bandwidth = 0.80$/شهر
- **الإجمالي: ~6.5$/شهر**

لا يزال رخيص جداً! ✅

---

## 🛠️ عن الإعداد

### س23: هل يجب أن يكون المشروع على GitHub؟

**ج:**

**Vercel:** نعم ✅ (أو GitLab, Bitbucket)
- تحتاج Git repository للـ Auto-deploy

**Fly.io:** لا ❌
- يمكنك النشر من local folder مباشرة
- لكن موصى به: استخدمي Git للـ version control

---

### س24: ماذا لو كان Backend في مجلد فرعي؟

**ج:**

إذا كان المشروع:
```
project/
├── frontend/
│   ├── src/
│   └── package.json
└── backend/
    ├── src/
    └── package.json
```

**في fly.toml:**
```toml
[build]
  dockerfile = "backend/Dockerfile"
```

**أو:** انقلي `fly.toml` و `Dockerfile` إلى مجلد `backend/`

---

### س25: كيف أحذف التطبيق من Fly.io أو Vercel؟

**ج:**

**Fly.io:**
```bash
fly apps destroy eishro-backend
```

سيطلب تأكيد - اكتبي اسم التطبيق.

**Vercel:**
1. Dashboard → اختاري المشروع
2. Settings → Advanced
3. **Delete Project**
4. اكتبي اسم المشروع للتأكيد

---

## 🔧 مشاكل شائعة

### س26: الصور لا تُرفع - ما الحل؟

**ج:**

**تحققي من:**

1. **حجم الملف:**
   - Backend limit: 50MB (في `app.ts`)
   - Fly.io limit: حسب Memory

2. **مجلد Uploads:**
   ```bash
   fly ssh console
   ls -la /app/uploads
   chmod 755 /app/uploads
   ```

3. **Persistent Storage:**
   إذا الصور تُحذف بعد Redeploy، أنشئي Volume:
   ```bash
   fly volumes create uploads_volume --size 3
   ```

   ثم في `fly.toml`:
   ```toml
   [[mounts]]
     source = "uploads_volume"
     destination = "/app/uploads"
   ```

---

### س27: Payment Gateway لا يعمل - ما السبب؟

**ج:**

**الأسباب المحتملة:**

1. **Moamalat Credentials خاطئة:**
   ```bash
   fly secrets list
   ```
   تحققي من:
   - MOAMALAT_MID
   - MOAMALAT_TID
   - MOAMALAT_SECRET

2. **CORS Issue:**
   - Moamalat Lightbox يفتح في iframe
   - يحتاج CORS صحيح

3. **HTTPS Required:**
   - Moamalat يطلب HTTPS
   - Fly.io يوفره تلقائياً ✅

**الحل:**
```bash
# اختبر endpoint
curl https://eishro-backend.fly.dev/api/payment/moamalat/test

# راجع logs
fly logs | grep moamalat
```

---

### س28: CORS Error مستمر - ماذا أفعل؟

**ج:**

**خطوات الإصلاح:**

1. **تحققي من Frontend URL في Backend:**
   ```bash
   fly secrets list
   ```

   يجب أن يكون:
   ```
   FRONTEND_URL=https://your-exact-vercel-url.vercel.app
   CORS_ORIGIN=https://your-exact-vercel-url.vercel.app
   ```

   **مهم:** بدون `/` في النهاية!

2. **تحققي من CORS config في `app.ts`:**
   ```typescript
   const corsOptions = {
     origin: process.env.FRONTEND_URL,
     credentials: true
   };
   app.use(cors(corsOptions));
   ```

3. **أعد نشر Backend:**
   ```bash
   fly deploy
   ```

4. **انتظري 1 دقيقة** ثم اختبري

5. **Clear Cache في المتصفح:**
   - Ctrl+Shift+R (Hard Reload)

---

## 📱 عن Custom Domain

### س29: كيف أستخدم نطاقي الخاص؟

**ج:**

**للـ Frontend (Vercel):**

1. Vercel Dashboard → Settings → Domains
2. أضيفي: `www.ishro.ly`
3. في CPanel → DNS:
   ```
   Type: CNAME
   Name: www
   Value: cname.vercel-dns.com
   TTL: Automatic
   ```
4. انتظري 5-10 دقائق

**للـ Backend (Fly.io):**

```bash
fly certs add api.ishro.ly
```

ثم في CPanel → DNS:
```
Type: CNAME
Name: api
Value: eishro-backend.fly.dev
TTL: Automatic
```

**النتيجة:**
```
Frontend: https://www.ishro.ly
Backend: https://api.ishro.ly
```

---

### س30: SSL Certificate - هل أحتاج شراءه؟

**ج:** لا! ❌

**Vercel:** SSL مجاني تلقائياً ✅ (Let's Encrypt)  
**Fly.io:** SSL مجاني تلقائياً ✅ (Let's Encrypt)  

**حتى للـ Custom Domains:** مجاني! ✅

---

## 🔄 عن Backup & Recovery

### س31: كيف أحفظ نسخة احتياطية؟

**ج:**

**Database (MySQL):**

**الطريقة 1: من CPanel**
```
CPanel → Backup → Download MySQL Database Backup
```

**الطريقة 2: من CLI**
```bash
fly ssh console
mysqldump -h $DB_HOST -u $DB_USER -p$DB_PASSWORD $DB_NAME > backup.sql
```

**Code:**
- Git repository على GitHub ✅ (نسخة احتياطية تلقائية)

**Uploaded Files:**
- إذا استخدمت Fly.io Volume - مستمرة
- أو استخدمي S3/Cloudinary للتخزين الدائم

---

### س32: كيف أستعيد من Backup؟

**ج:**

**Database:**

**الطريقة 1: phpMyAdmin**
1. CPanel → phpMyAdmin
2. اختاري Database
3. Import → اختاري ملف `.sql`

**الطريقة 2: CLI**
```bash
mysql -h $DB_HOST -u $DB_USER -p$DB_PASSWORD $DB_NAME < backup.sql
```

**Code:**
```bash
git checkout <commit-hash>
fly deploy
```

---

## 🌍 عن الأداء العالمي

### س33: مستخدمينا من مناطق مختلفة - كيف نحسن السرعة؟

**ج:**

**Frontend (Vercel):**
- ✅ CDN عالمي تلقائياً
- المستخدمون يحصلون على أسرع نسخة من أقرب server

**Backend (Fly.io):**

**الطريقة 1: إضافة Regions**
```bash
# إضافة London للمستخدمين من أوروبا
fly regions add lhr

# إضافة Dubai للمستخدمين من الخليج
fly regions add dxb

# عرض جميع Regions
fly regions list
```

**الطريقة 2: Read Replicas للـ Database**
- أنشئي MySQL replicas في مناطق مختلفة
- Fly.io يوجه كل request لأقرب region

---

### س34: كيف أراقب الأداء؟

**ج:**

**Fly.io Metrics:**
```bash
fly dashboard
```

في Dashboard:
- **CPU Usage:** يجب < 80%
- **Memory Usage:** يجب < 80%
- **Network:** Requests/second

**Vercel Analytics:**
- Dashboard → Analytics
- **Performance Score**
- **Response Time**
- **Error Rate**

**إذا الأداء سيئ:**
- زيادة Resources
- Caching
- Database optimization

---

## 🎓 نصائح متقدمة

### س35: كيف أحسن استهلاك Free Tier؟

**ج:**

**Fly.io:**

1. **استخدمي 1 VM فقط** (مجاني):
   ```bash
   fly scale count 1
   ```

2. **قللي Memory** (إذا كافي):
   ```bash
   fly scale memory 256
   ```

3. **راقبي Bandwidth:**
   ```bash
   fly dashboard → Usage
   ```

4. **فعلي Compression:**
   ```typescript
   app.use(compression());
   ```

**Vercel:**
- Unlimited في Hobby - لا قلق! ✅

---

## 📞 الحصول على مساعدة

### س36: أين أطلب المساعدة إذا علقت؟

**ج:**

**Fly.io:**
- **Docs:** https://fly.io/docs (شامل جداً)
- **Community:** https://community.fly.io (رد سريع)
- **Discord:** https://fly.io/discord (مجتمع نشط)

**Vercel:**
- **Docs:** https://vercel.com/docs (ممتاز)
- **Discord:** https://vercel.com/discord
- **Twitter:** @vercel (رد أحياناً)

**CPanel/MySQL:**
- **دعم مزود الاستضافة** (عبر تذكرة)

**عام:**
- **Stack Overflow:** ضعي tag [fly.io] أو [vercel]
- **Reddit:** r/webdev

---

**آخر تحديث:** ديسمبر 2025
