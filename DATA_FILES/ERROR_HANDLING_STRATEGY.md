# 🚨 استراتيجية معالجة الأخطاء الشاملة

**آخر تحديث:** 6 ديسمبر 2025  
**النسخة:** 1.0

---

## 📋 جدول الأخطاء المحتملة

### الفئة 1: أخطاء المصادقة والتفويض

```
ERROR: Unauthorized (401)
├─ السبب:
│  ├─ لم يتم تسجيل الدخول
│  ├─ انتهت جلسة العمل
│  └─ التوكن منتهي الصلاحية
│
├─ الرسالة المعروضة:
│  └─ "يجب عليك تسجيل الدخول أولاً"
│
├─ الإجراء:
│  ├─ إعادة توجيه إلى صفحة تسجيل الدخول
│  ├─ مسح البيانات المخزنة
│  └─ طلب إعادة المصادقة
│
└─ Code Implementation:
   if (response.status === 401) {
     localStorage.clear();
     navigate('/login');
     showNotification('جلستك انتهت. يرجى تسجيل الدخول مجدداً');
   }

ERROR: Forbidden (403)
├─ السبب:
│  ├─ لا توجد صلاحيات كافية
│  ├─ حاول الوصول لموارد محمية
│  └─ دور المستخدم غير كافي
│
├─ الرسالة المعروضة:
│  └─ "ليس لديك صلاحيات للوصول إلى هذا الجزء"
│
├─ الإجراء:
│  ├─ عدم السماح بالوصول
│  ├─ إعادة توجيه لصفحة البداية
│  └─ تسجيل المحاولة (Log)
│
└─ Code:
   if (response.status === 403) {
     logSecurityEvent('unauthorized_access_attempt', {
       user_id, target, timestamp
     });
     navigate('/');
   }
```

### الفئة 2: أخطاء النماذج والبيانات

```
ERROR: Bad Request (400)
├─ السبب:
│  ├─ بيانات غير صحيحة في الطلب
│  ├─ حقل مفقود أو فارغ
│  ├─ صيغة البريد الإلكتروني غير صحيحة
│  ├─ رقم الهاتف غير صحيح
│  └─ القيمة الرقمية خارج النطاق المسموح
│
├─ أمثلة على الأخطاء:
│  ├─ Email validation failed
│  ├─ Phone number must be 10-15 digits
│  ├─ Price must be > 0
│  ├─ Quantity must be integer
│  └─ Address is required
│
├─ الرسالة المعروضة (للمستخدم):
│  └─ "البيانات المدخلة غير صحيحة. يرجى التحقق:"
│     └─ عرض الحقول المعيبة بلون أحمر
│
├─ الإجراء:
│  ├─ تسليط الضوء على الحقول الخاطئة
│  ├─ عرض رسائل توضيحية
│  ├─ منع إرسال النموذج
│  └─ إعادة المحاولة
│
└─ Frontend Validation:
   function validateOrderData(data) {
     const errors = {};
     
     if (!data.firstName?.trim()) {
       errors.firstName = 'الاسم الأول مطلوب';
     }
     if (!isValidEmail(data.email)) {
       errors.email = 'البريد الإلكتروني غير صحيح';
     }
     if (!isValidPhone(data.phone)) {
       errors.phone = 'رقم الهاتف يجب أن يكون 10-15 رقم';
     }
     
     return errors;
   }

ERROR: Duplicate Entry (409)
├─ السبب:
│  ├─ محاولة إنشاء بريد إلكتروني موجود
│  ├─ رقم طلب مكرر
│  └─ كوبون مستخدم بالفعل
│
├─ الرسالة المعروضة:
│  └─ "هذا البريد الإلكتروني مسجل بالفعل"
│
├─ الإجراء:
│  ├─ اقترح إجراء بديل (مثل تسجيل دخول)
│  ├─ امسح الحقل وأعد المحاولة
│  └─ انتقل لصفحة البيانات الموجودة
│
└─ Code:
   if (error.message.includes('Duplicate')) {
     if (error.field === 'email') {
       showError('هذا البريد الإلكتروني مسجل بالفعل');
       suggestOption('هل تريد تسجيل الدخول؟');
     }
   }
```

### الفئة 3: أخطاء قاعدة البيانات

```
ERROR: Not Found (404)
├─ السبب:
│  ├─ المنتج لا يوجد
│  ├─ المتجر لا يوجد
│  ├─ الطلب غير موجود
│  └─ الصفحة غير موجودة
│
├─ الرسالة المعروضة:
│  └─ "المنتج الذي تبحث عنه غير موجود"
│
├─ الإجراء:
│  ├─ عرض رسالة ودية
│  ├─ اقتراح منتجات بديلة
│  ├─ زر "العودة" أو "المتابعة للتسوق"
│  └─ عدم تسجيل كخطأ حرج
│
└─ UX Example:
   ┌─────────────────────────────┐
   │ 🔍 المنتج غير موجود        │
   │                             │
   │ الآسف! المنتج الذي تبحث عنه│
   │ غير متاح حالياً             │
   │                             │
   │ [← العودة] [اقتراحات ⭐]    │
   └─────────────────────────────┘

ERROR: Database Connection Failed
├─ السبب:
│  ├─ خادم قاعدة البيانات معطل
│  ├─ شبكة المؤسسة مقطوعة
│  ├─ انتهت اتصالات قاعدة البيانات
│  └─ استعلام طويل جداً
│
├─ الرسالة المعروضة:
│  └─ "نواجه مشكلة فنية. يرجى المحاولة لاحقاً"
│
├─ الإجراء:
│  ├─ تسجيل الخطأ (Log)
│  ├─ إرسال تنبيه للمسؤول
│  ├─ عرض رسالة عامة للمستخدم
│  ├─ إعادة محاولة تلقائية بعد X ثانية
│  └─ صفحة maintenance (إذا كانت قطعة كاملة)
│
└─ Code:
   const MAX_RETRIES = 3;
   async function fetchWithRetry(url, options, retryCount = 0) {
     try {
       return await fetch(url, options);
     } catch (error) {
       if (retryCount < MAX_RETRIES) {
         await new Promise(r => setTimeout(r, 1000 * (retryCount + 1)));
         return fetchWithRetry(url, options, retryCount + 1);
       }
       throw new DatabaseConnectionError();
     }
   }

ERROR: Data Integrity Violation
├─ السبب:
│  ├─ حذف منتج لازال مستخدم في طلبات
│  ├─ انتهاك علاقة جدول
│  ├─ انتهاك قيد فريد (Unique)
│  └─ انتهاك قيمة فارغة (NOT NULL)
│
├─ الرسالة المعروضة (للإدارة):
│  └─ "لا يمكن حذف هذا المنتج لأنه موجود في طلبات سابقة"
│
├─ الإجراء:
│  ├─ منع العملية
│  ├─ اقتراح حل بديل (تعطيل بدلاً من الحذف)
│  └─ تسجيل في السجلات (Audit Log)
│
└─ Code:
   try {
     await Product.destroy({ where: { id } });
   } catch (error) {
     if (error.name === 'SequelizeForeignKeyConstraintError') {
       throw new CustomError('Cannot delete product that has active orders');
     }
   }
```

### الفئة 4: أخطاء الدفع

```
ERROR: Payment Declined (4000)
├─ السبب:
│  ├─ بطاقة منتهية الصلاحية
│  ├─ رصيد غير كافي
│  ├─ بيانات البطاقة خاطئة
│  └─ تحذير من البنك (احتيال)
│
├─ الرسالة المعروضة:
│  └─ "تم رفض الدفع. يرجى التحقق من بيانات البطاقة"
│
├─ الإجراء:
│  ├─ عدم إنشاء الطلب
│  ├─ حفظ السلة للمحاولة لاحقاً
│  ├─ عرض خيارات بديلة (بطاقة أخرى / COD)
│  └─ لا تقل أرقام البطاقة الحقيقية
│
└─ Code:
   const moamalatResponse = await openMoamalatLightbox({...});
   
   if (moamalatResponse.status === 'DECLINED') {
     showNotification({
       type: 'error',
       message: 'تم رفض الدفع. التفاصيل: ' + moamalatResponse.reason,
       actions: [
         { label: 'إعادة محاولة', action: retryPayment },
         { label: 'الدفع عند الاستلام', action: switchToCOD },
         { label: 'بطاقة أخرى', action: selectDifferentCard }
       ]
     });
   }

ERROR: Payment Timeout (5000)
├─ السبب:
│  ├─ اتصال الإنترنت بطيء
│  ├─ خادم الدفع بطيء
│  ├─ المستخدم أغلق الصفحة
│  └─ انقطاع الإنترنت
│
├─ الرسالة المعروضة:
│  └─ "عملية الدفع استغرقت وقتاً طويلاً. يرجى المحاولة مجدداً"
│
├─ الإجراء:
│  ├─ التحقق من حالة الطلب (لم يتم فرض رسوم؟)
│  ├─ إعادة محاولة الدفع
│  ├─ إظهار رقم الطلب للاتصال بالدعم
│  └─ منع الرسوم المزدوجة
│
└─ Code:
   const PAYMENT_TIMEOUT = 30000; // 30 seconds
   
   const paymentPromise = openMoamalatLightbox({...});
   const timeoutPromise = new Promise((_, reject) =>
     setTimeout(() => reject(new Error('Payment timeout')), PAYMENT_TIMEOUT)
   );
   
   try {
     const result = await Promise.race([paymentPromise, timeoutPromise]);
   } catch (error) {
     if (error.message === 'Payment timeout') {
       await checkOrderStatus(); // Verify no charge occurred
       showRetryOption();
     }
   }

ERROR: Duplicate Payment (4001)
├─ السبب:
│  ├─ المستخدم نقر الزر مرتين
│  ├─ المستخدم أعاد تحميل الصفحة أثناء الدفع
│  └─ خطأ في المعالجة
│
├─ الرسالة المعروضة:
│  └─ "هذا الطلب تمت معالجته بالفعل"
│
├─ الإجراء:
│  ├─ منع الرسوم المزدوجة (Idempotent)
│  ├─ استخدام transaction_id فريد
│  ├─ عرض الطلب الموجود
│  └─ عدم إنشاء طلب جديد
│
└─ Code (Idempotent):
   const ORDER_LOCK_TIME = 60000; // 1 minute
   const recentOrders = await Order.find({
     user_id,
     created_at: { $gte: Date.now() - ORDER_LOCK_TIME },
     status: 'confirmed'
   });
   
   if (recentOrders.length > 0) {
     return showExistingOrder(recentOrders[0]);
   }
```

### الفئة 5: أخطاء المخزون والتوفرية

```
ERROR: Out of Stock (4020)
├─ السبب:
│  ├─ آخر وحدة تم شراؤها قبل لحظات
│  ├─ خطأ في تحديث المخزون
│  ├─ منتج موسمي نفد
│  └─ تزامن race condition
│
├─ الرسالة المعروضة:
│  └─ "للأسف، هذا المنتج نفد من المخزون حالياً"
│
├─ الإجراء:
│  ├─ إزالة من السلة
│  ├─ عرض بدائل مشابهة
│  ├─ خيار: "أخبرني عند العودة"
│  ├─ عرض تاريخ العودة المتوقع
│  └─ متابعة الطلب بدون هذا العنصر
│
└─ UX Flow:
   if (product.quantity_stock === 0) {
     showNotification({
       type: 'warning',
       title: 'المنتج نفد من المخزون',
       actions: [
         { label: 'إزالة من السلة', action: removeFromCart },
         { label: 'أخبرني عند العودة', action: setNotification },
         { label: 'عرض بدائل', action: showAlternatives }
       ]
     });
   }

ERROR: Partial Stock (4021)
├─ السبب:
│  └─ طلب 5 وحدات لكن يوجد 2 فقط
│
├─ الرسالة المعروضة:
│  └─ "يتوفر فقط 2 وحدة من أصل 5"
│
├─ الإجراء:
│  ├─ اسأل: هل تقبل بـ 2 وحدة فقط؟
│  ├─ أو انتظر توفر كمية أكبر
│  ├─ أو احذف من السلة
│  └─ احسب السعر على أساس الكمية المتاحة
│
└─ Code:
   if (requestedQty > availableQty && availableQty > 0) {
     showConfirmDialog(
       `يتوفر فقط ${availableQty} وحدة. هل تقبل؟`,
       () => updateCartQty(availableQty),
       () => removeFromCart()
     );
   }
```

### الفئة 6: أخطاء الشحن والتوصيل

```
ERROR: Invalid Address (4030)
├─ السبب:
│  ├─ عنوان غير موجود
│  ├─ المنطقة غير مخدومة
│  ├─ رقم المنزل غير صحيح
│  └─ مدينة غير مدعومة
│
├─ الرسالة المعروضة:
│  └─ "لا نستطيع الشحن إلى هذا العنوان"
│
├─ الإجراء:
│  ├─ اطلب عنوان بديل
│  ├─ عرض المناطق المدعومة
│  ├─ خيار اختيار من موقع على الخريطة
│  └─ اتصال بخدمة العملاء
│
└─ Code:
   const supportedCities = ['طرابلس', 'بنغازي', 'مصراتة'];
   
   if (!supportedCities.includes(selectedCity)) {
     showError('نعتذر! لا نوصل حالياً إلى ' + selectedCity);
     showAlternative('المناطق المدعومة: ' + supportedCities.join(', '));
   }

ERROR: Delivery Failed (4031)
├─ السبب:
│  ├─ المستقبل غير متوفر
│  ├─ العنوان غير صحيح بالفعل
│  ├─ عدم إمكانية الدخول
│  └─ شخص آخر استقبل الطلب
│
├─ الرسالة المعروضة:
│  └─ "فشلت عملية التسليم"
│
├─ الإجراء:
│  ├─ إرسال إشعار فوري للعميل
│  ├─ تقديم خيارات:
│  │  ├─ إعادة محاولة التسليم
│  │  ├─ استلام من الفرع
│  │  └─ استرجاع المنتج
│  ├─ معالجة استرجاع الأموال
│  └─ تسجيل الحادث
│
└─ Code:
   if (delivery.status === 'FAILED') {
     notifyCustomer({
       title: 'فشل التسليم',
       message: delivery.failureReason,
       options: [
         'إعادة التسليم',
         'استلام من الفرع',
         'إرجاع المنتج'
       ]
     });
   }
```

### الفئة 7: أخطاء الخدمات الخارجية

```
ERROR: Minimax API Unavailable (5000)
├─ السبب:
│  ├─ خادم Minimax معطل
│  ├─ انقطاع الإنترنت
│  ├─ API key غير صحيح
│  └─ حد الطلبات تجاوز
│
├─ الرسالة المعروضة:
│  └─ "خدمة الدعم الذكي غير متاحة حالياً"
│
├─ الإجراء:
│  ├─ استخدام Fallback response
│  ├─ عرض دعم بشري بديل
│  ├─ تسجيل الخطأ
│  └─ إعادة محاولة بعد X ثانية
│
└─ Code (من enhancedApi.ts):
   async callMinimaxTool(toolCall) {
     if (!MINIMAX_API_CONFIG.enabled) {
       return this.getFallbackResponse(toolCall);
     }
     
     try {
       const response = await this.requestWithRetry(...);
       if (!response.success) {
         return this.getFallbackResponse(toolCall);
       }
       return response;
     } catch (error) {
       return this.getFallbackResponse(toolCall);
     }
   }

ERROR: Moamalat Gateway Unavailable (5001)
├─ السبب:
│  ├─ خادم Moamalat معطل
│  ├─ مشكلة في البنك
│  ├─ صيانة مجدولة
│  └─ معدل الأخطاء عالي
│
├─ الرسالة المعروضة:
│  └─ "خدمة الدفع غير متاحة حالياً"
│
├─ الإجراء:
│  ├─ عرض خيار الدفع عند الاستلام فقط
│  ├─ إظهار رسالة قادمة قريباً
│  ├─ وضع المستخدم على قائمة انتظار
│  └─ إخطار العمليات
│
└─ Code:
   if (!isMoamalatAvailable) {
     disablePaymentMethod('immediate');
     showNotification('الدفع الفوري غير متاح. استخدم الدفع عند الاستلام');
     saveOrderAsPending();
   }

ERROR: AWS S3 Upload Failed (5002)
├─ السبب:
│  ├─ حد التخزين تجاوز
│  ├─ صيغة الملف غير مدعومة
│  ├─ الملف كبير جداً
│  └─ مشكلة في الاتصال
│
├─ الرسالة المعروضة:
│  └─ "فشل رفع الصورة. يرجى المحاولة مجدداً"
│
├─ الإجراء:
│  ├─ اقتراح ضغط الصورة
│  ├─ التحقق من صيغة الملف (JPG/PNG)
│  ├─ إعادة المحاولة
│  └─ استخدام صورة افتراضية مؤقتة
│
└─ Validation:
   const MAX_FILE_SIZE = 5 * 1024 * 1024; // 5MB
   const ALLOWED_TYPES = ['image/jpeg', 'image/png', 'image/webp'];
   
   if (file.size > MAX_FILE_SIZE) {
     showError('الملف أكبر من 5MB. يرجى ضغطه');
     return;
   }
   
   if (!ALLOWED_TYPES.includes(file.type)) {
     showError('صيغة الملف غير مدعومة. استخدم JPG أو PNG');
     return;
   }
```

---

## 🛡️ استراتيجيات التعامل مع الأخطاء

### 1. Try-Catch Pattern

```typescript
try {
  const data = await fetchData();
  processData(data);
} catch (error) {
  if (error instanceof ValidationError) {
    handleValidationError(error);
  } else if (error instanceof NetworkError) {
    handleNetworkError(error);
    retryWithBackoff();
  } else if (error instanceof ServerError) {
    handleServerError(error);
    reportToAdmin();
  } else {
    handleUnexpectedError(error);
  }
} finally {
  cleanupResources();
}
```

### 2. Error Boundary (React)

```typescript
class ErrorBoundary extends React.Component {
  componentDidCatch(error, errorInfo) {
    logErrorToService(error, errorInfo);
    this.setState({ hasError: true });
  }

  render() {
    if (this.state.hasError) {
      return <ErrorFallback retry={() => this.setState({ hasError: false })} />;
    }
    return this.props.children;
  }
}
```

### 3. API Error Response Handler

```typescript
async function handleApiError(response) {
  const statusCode = response.status;
  const data = await response.json();

  switch (statusCode) {
    case 400:
      return { type: 'validation', message: data.message, fields: data.fields };
    case 401:
      return { type: 'auth', message: 'Please login' };
    case 403:
      return { type: 'permission', message: 'Access denied' };
    case 404:
      return { type: 'notfound', message: 'Resource not found' };
    case 500:
      return { type: 'server', message: 'Server error' };
    default:
      return { type: 'unknown', message: 'Unknown error' };
  }
}
```

### 4. User Notification Pattern

```typescript
function showErrorNotification(error) {
  const notifications = {
    validation: {
      title: '❌ خطأ في البيانات',
      color: 'red',
      duration: 5000
    },
    network: {
      title: '🌐 مشكلة في الاتصال',
      color: 'orange',
      duration: 10000
    },
    server: {
      title: '🔥 خطأ في الخادم',
      color: 'red',
      duration: 7000
    },
    success: {
      title: '✅ تمت العملية بنجاح',
      color: 'green',
      duration: 3000
    }
  };

  const config = notifications[error.type] || notifications.unknown;
  toast(config.title, config);
}
```

---

## 📊 جدول ملخص الأخطاء

| رمز الخطأ | الوصف | الحالة | الإجراء |
|---------|-------|-------|--------|
| 400 | Bad Request | User Error | Validate Input |
| 401 | Unauthorized | Auth Error | Re-login |
| 403 | Forbidden | Permission Error | Show Denied |
| 404 | Not Found | Data Missing | Show Alternatives |
| 409 | Conflict | Duplicate | Show Existing |
| 500 | Server Error | Server Down | Retry Later |
| 503 | Service Down | Maintenance | Show Maintenance |
| Network | No Connection | Offline | Offline Mode |

---

## ✅ أفضل الممارسات

1. ✅ **عدم تسرب تفاصيل تقنية** - لا تظهر stack traces للمستخدم
2. ✅ **رسائل واضحة ودية** - باللغة العربية
3. ✅ **الإجراءات البديلة** - كبّر خيارات للمستخدم
4. ✅ **التسجيل الشامل** - Log الأخطاء للتحليل
5. ✅ **إعادة محاولة ذكية** - استخدم exponential backoff
6. ✅ **Graceful degradation** - استمر حتى مع الأخطاء

---

**آخر تحديث:** 6 ديسمبر 2025  
**الحالة:** ✅ شامل ومُحدّث
