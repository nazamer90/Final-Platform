# اختبار النشر محلياً - قبل النشر على الخوادم

## 🧪 اختبار بناء Docker محلياً:

قبل نشر Backend على Fly.io، اختبره محلياً باستخدام Docker.

### المرحلة 1: إعداد Docker:

- تثبيت Docker من: https://www.docker.com/products/docker-desktop

### المرحلة 2: بناء الـ Image:

```cmd
# الدخول لمجلد Backend
cd backend

# بناء الـ Image
docker build -t eishro-backend:latest .

# قد يستغرق هذا 5-10 دقائق في المرة الأولى
```

### المرحلة 3: تشغيل الـ Container:

```cmd
# تشغيل الـ Container
docker run -it \
  -p 8080:8080 \
  -e NODE_ENV=development \
  -e PORT=8080 \
  -e DB_HOST=host.docker.internal \
  -e DB_PORT=3306 \
  -e DB_USER=root \
  -e DB_PASSWORD=your_password \
  -e DB_NAME=eishro_db \
  -e JWT_SECRET=test_secret_key_here_12345678901234567890 \
  -e SESSION_SECRET=test_session_secret_12345678901234567890 \
  -e ENCRYPTION_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef \
  eishro-backend:latest
```

### المرحلة 4: اختبار التطبيق:

افتح المتصفح وادخل:

```
http://localhost:8080/api/health
```

يجب أن تحصل على استجابة JSON توضح أن الخادم يعمل.

### المرحلة 5: إيقاف الـ Container:

```cmd
# اضغط Ctrl + C في Terminal
```

---

## 🔍 اختبار Frontend محلياً:

### المرحلة 1: تثبيت الاعتماديات:

```cmd
# من مجلد المشروع الرئيسي
npm install

# أو إذا كان لديك yarn
yarn install
```

### المرحلة 2: تشغيل الخادم المحلي:

```cmd
# تشغيل Vite dev server
npm run dev
```

سيُطبع لك URL محلي (عادة `http://localhost:5174`)

### المرحلة 3: اختبار الاتصال بـ Backend:

تأكد من أن متغيرات البيئة تشير إلى Backend المحلي:

في `.env`:
```
VITE_API_URL=http://localhost:5000/api
VITE_BACKEND_URL=http://localhost:5000
```

أو إذا كان Backend يعمل على Docker:
```
VITE_API_URL=http://localhost:8080/api
VITE_BACKEND_URL=http://localhost:8080
```

### المرحلة 4: الاختبار:

1. افتح http://localhost:5174 في المتصفح
2. افتح Chrome DevTools (F12)
3. اذهب إلى Network tab
4. جرب أي عملية تتطلب API call
5. تأكد من:
   - [ ] لا توجد CORS errors
   - [ ] API responses صحيحة
   - [ ] البيانات تُحفظ بنجاح

---

## 🧩 اختبار عام متكامل:

### شغّل كل شيء محلياً:

```cmd
# Terminal 1: قاعدة البيانات (MySQL)
# تأكد من أن MySQL يعمل

# Terminal 2: Backend
cd backend
npm run dev

# Terminal 3: Frontend
npm run dev
```

### اختبر بعض العمليات الأساسية:

- [ ] تسجيل مستخدم جديد
- [ ] تسجيل الدخول
- [ ] عرض المنتجات
- [ ] البحث في المنتجات
- [ ] إضافة منتج للسلة
- [ ] الدفع (في بيئة الاختبار)
- [ ] تحميل الملفات (صور، إلخ)

---

## ✅ قبل النشر:

- [ ] اختبار البناء Docker نجح بدون أخطاء
- [ ] التطبيق يعمل محلياً بدون مشاكل
- [ ] الاتصال بـ Database يعمل
- [ ] API requests تستجيب
- [ ] File uploads تعمل
- [ ] Sessions تعمل بشكل صحيح
- [ ] لا توجد أخطاء في Console

---

## 🚨 استكشاف الأخطاء:

### Docker build فشل:

```cmd
# اختبر البناء مع verbose output
docker build -t eishro-backend:latest . --progress=plain
```

### Container لا يعمل:

```cmd
# اعرض السجلات
docker logs <container-id>

# أو اعرض جميع الـ containers
docker ps -a
```

### Frontend لا يتصل بـ Backend:

- [ ] تأكد من أن Backend يعمل
- [ ] تأكد من متغيرات البيئة صحيحة
- [ ] افتح DevTools وانظر للأخطاء في Console

---

**عندما كل شيء يعمل محلياً، أنت جاهز للنشر على الخوادم السحابية! 🚀**
