# حل مشكلة عدم عرض قوالب الإعلانات

## ✅ التغييرات التي تم إجراؤها

### 1. إصلاح Vite Asset Sync Plugin (vite.config.ts)
- تم تحسين آلية مزامنة مجلد AdsForms
- تم تغيير الخطاف من `config()` إلى `configResolved()` لضمان تنفيذ المزامنة بشكل صحيح
- سيتم نسخ ملفات الصور تلقائياً من `/AdsForms` إلى `/public/AdsForms` عند بدء خادم التطوير

### 2. تحسين معالجة تحميل الصور (AdsManagementView.tsx)
- تم تحسين الرسائل في console عند فشل تحميل الصور
- تم تحسين الخلفية (من رمادي فاتح إلى أسود داكن) لتوفير تباين أفضل
- سيتم عرض رسالة واضحة عند عدم توفر الصور

## 🔧 الخطوات المطلوبة

### الخطوة 1: توقيف خادم التطوير الحالي
إذا كان خادم التطوير قيد التشغيل:
- اضغط `Ctrl + C` في Terminal
- انتظر توقف الخادم بالكامل

### الخطوة 2: مسح الذاكرة المؤقتة
```bash
rm -rf dist
rm -rf node_modules/.vite
```

أو على Windows:
```bash
rmdir /s /q dist
rmdir /s /q node_modules\.vite
```

### الخطوة 3: بدء خادم التطوير من جديد
```bash
npm run dev
```

ستشاهد الرسالة: `✓ AdsForms synced to public directory` في console

### الخطوة 4: فتح التطبيق
- افتح المتصفح على `http://localhost:5174`
- انتقل إلى إدارة الإعلانات
- يجب أن تظهر صور قوالب الإعلانات الآن

## 📊 التحقق من الملفات

جميع ملفات الصور موجودة في `/public/AdsForms/`:
- ✓ adv1.jpg (313.92 KB)
- ✓ adv2.jpg (2.27 MB)
- ✓ adv3.jpg (119.71 KB)
- ✓ adv4.jpg (280.94 KB)
- ✓ adv5.jpg (148.19 KB)
- ✓ adv6.jpg (83.82 KB)
- ✓ adv7.jpg (356.15 KB)
- ✓ adv8.jpg (180.39 KB)
- ✓ adv9.jpg (82.82 KB)
- ✓ adv10.jpg (154.45 KB)
- ✓ adv11.jpg (478.64 KB)

جميع القوالب موجودة في `src/data/adTemplates.ts` مع المسارات الصحيحة.

## 🐛 استكشاف الأخطاء

إذا لم تظهر الصور بعد اتباع الخطوات أعلاه:

### 1. تحقق من Browser Console (F12)
ابحث عن رسائل مثل:
- `Failed to load: /AdsForms/adv1.jpg`

### 2. تحقق من أن الملفات موجودة
```bash
ls -la public/AdsForms/
```

### 3. تحقق من عنوان الصور
- جرّب فتح `http://localhost:5174/AdsForms/adv1.jpg` مباشرة في المتصفح
- يجب أن ترى الصورة

### 4. مسح الـ Cache
- اضغط `Ctrl + Shift + Delete` للوصول إلى خيارات تنظيف البيانات
- امسح الـ cache والـ cookies
- أعد تحميل الصفحة

## 📝 ملاحظات تقنية

- **المسارات**: جميع الصور تستخدم مسارات مطلقة (`/AdsForms/adv[1-11].jpg`)
- **Vite Public Folder**: تُعامل الملفات في `public/` كملفات ثابتة وتُخدم من الجذر (`/`)
- **الحجم**: أبعاد الصور المحسّنة: 1920×450px
- **الصيغة**: جميع الصور بصيغة JPG

## ✨ ما بعد الإصلاح

بمجرد ظهور الصور:
1. يمكنك إنشاء إعلانات جديدة باستخدام القوالب
2. سيتم عرض معاينة الصور في لوحة التحكم
3. الإعلانات ستُعرض بشكل صحيح في المتجر مع الصور الكاملة
