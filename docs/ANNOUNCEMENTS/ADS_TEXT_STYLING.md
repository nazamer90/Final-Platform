# نظام تنسيق النصوص في الإعلانات

## المميزات الجديدة ✨

تم إضافة إمكانيات متقدمة للتحكم في ظهور النصوص على الإعلانات:

### 1️⃣ تموضع النص (Text Position)
**9 خيارات مختلفة:**
- **أعلى اليسار** (top-left)
- **أعلى الوسط** (top-center)  
- **أعلى اليمين** (top-right)
- **يسار الوسط** (center-left)
- **الوسط** (center) - الخيار الافتراضي
- **يمين الوسط** (center-right)
- **أسفل اليسار** (bottom-left)
- **أسفل الوسط** (bottom-center)
- **أسفل اليمين** (bottom-right)

### 2️⃣ لون النص (Text Color)
- **منتقي ألوان كامل** يسمح باختيار أي لون RGB
- **معاينة فورية** للون المختار
- **افتراضي**: أبيض (#ffffff)
- يدعم جميع صيغ الألوان (#RRGGBB)

### 3️⃣ خط النص (Text Font)
- **Cairo-SemiBold** - الخط الافتراضي المحسّن
- يدعم جميع أوزان خط Cairo:
  - Cairo-Regular
  - Cairo-Medium  
  - Cairo-SemiBold ⭐
  - Cairo-Bold
  - Cairo-ExtraBold
  - Cairo-Black

## المسار في واجهة التاجر

```
لوحة التاجر → المتجر → قسم الإعلانات → إضافة إعلان جديد
         ↓
الخطوة 1: أدخل العنوان والوصف
         ↓
📍 اختر موضع النص على القالب (9 خيارات)
🎨 اختر لون النص (منتقي ألوان)
```

## قاعدة البيانات

### جداول محدثة:
- **store_ads** (جدول الإعلانات)

### أعمدة جديدة:
| العمود | النوع | الافتراضي | الوصف |
|--------|-------|---------|---------|
| `text_position` | ENUM(9 options) | center | موضع النص على الإعلان |
| `text_color` | VARCHAR(7) | #ffffff | لون النص (بصيغة Hex) |
| `text_font` | VARCHAR(50) | Cairo-SemiBold | نوع الخط المستخدم |

## API Endpoints

### إنشاء إعلان بتنسيق النصوص:
```bash
POST /api/ads/store/{storeId}
Content-Type: application/json

{
  "templateId": "adv1",
  "title": "عنوان الإعلان",
  "description": "وصف الإعلان",
  "placement": "banner" | "between_products",
  "textPosition": "center",          // ← جديد
  "textColor": "#ffffff",            // ← جديد
  "textFont": "Cairo-SemiBold",      // ← جديد
  "linkUrl": "https://example.com"
}
```

### تحديث إعلان:
```bash
PUT /api/ads/store/{storeId}/{adId}
Content-Type: application/json

{
  "title": "عنوان جديد",
  "textPosition": "top-center",
  "textColor": "#000000",
  "textFont": "Cairo-Bold"
}
```

### الحصول على الإعلانات:
```bash
GET /api/ads/store/{storeId}
```

يتم إرجاع الإعلانات مع جميع خصائص التنسيق:
```json
{
  "success": true,
  "data": [
    {
      "id": 1,
      "templateId": "adv1",
      "title": "عنوان الإعلان",
      "description": "وصف الإعلان",
      "placement": "banner",
      "textPosition": "center",
      "textColor": "#ffffff",
      "textFont": "Cairo-SemiBold",
      "isActive": true,
      "createdAt": "2025-12-08T..."
    }
  ]
}
```

## عرض الإعلانات في المتجر

### شريط إعلاني (Banner)
- **يظهر**: أسفل الشريط العلوي مباشرة
- **النصوص**: تظهر فوق الصورة بالموضع واللون المختار
- **التفاعل**: قابل للنقر للذهاب إلى رابط الإعلان

### بين المنتجات (Between Products)
- **يظهر**: بين قائمة المنتجات (بعد كل 4 منتجات)
- **الحجم**: قرطة كاملة (Card)
- **النصوص**: تظهر فوق الصورة مع خيارات التموضع

## ملاحظات مهمة ⚠️

### التوافقية مع الألوان:
- إذا كان القالب أبيض اللون والنص أبيض:
  - **الحل**: اختر لون نص داكن (مثل أسود #000000 أو أزرق #0000FF)
  
- إذا كان القالب بألوان داكنة:
  - **الحل**: اختر لون نص فاتح (مثل أبيض #ffffff أو أصفر #FFFF00)

### اختيار الموضع:
- للإعلانات الضيقة: استخدم **top-center** أو **bottom-center**
- للإعلانات العريضة: استخدم **center-left** أو **center-right**
- للإعلانات الكبيرة: استخدم **center** (الوسط)

## الملفات المعدلة

### Frontend (React):
1. ✅ `src/components/AdsManagementView.tsx`
   - إضافة حقول اختيار موضع النص ولون النص
   - واجهة user-friendly للتحكم في التنسيق

2. ✅ `src/pages/StorePage.tsx`
   - إضافة دالة `getTextPositionStyle()` لحساب مواضع النص
   - تطبيق الألوان والخطوط على الإعلانات
   - دعم كلا نوعي الإعلانات (banner و between_products)

### Backend (Node.js):
1. ✅ `backend/src/models/StoreAd.ts`
   - إضافة attributes جديدة في Interface
   - إضافة field definitions في Sequelize

2. ✅ `backend/src/database/migrate.ts`
   - تحديث جداول MySQL و SQLite
   - إضافة أعمدة جديدة مع القيم الافتراضية

3. ✅ `backend/src/controllers/adController.ts`
   - تحديث `createStoreAd()` لقبول الحقول الجديدة
   - تحديث `updateStoreAd()` لتحديث التنسيق

### Styling:
- ✅ استخدام خط **Cairo-SemiBold** (موجود بالفعل في الموقع)
- ✅ Tailwind CSS classes للتموضع
- ✅ Inline styles للألوان والخطوط

## مثال عملي

### إنشاء إعلان احترافي:

**من واجهة التاجر:**

1. اختر "شريط إعلاني"
2. أدخل العنوان: "عرض خاص 50% على جميع المنتجات"
3. أدخل الوصف: "لفترة محدودة فقط"
4. **اختر موضع النص**: "أسفل الوسط" (bottom-center)
5. **اختر لون النص**: أحمر (#FF0000) للجذب
6. اختر قالب بصري مناسب
7. اضبط رابط الإعلان
8. انشر الإعلان ✅

**النتيجة**: إعلان احترافي مع نص واضح وملفت للنظر!

## Troubleshooting

### النصوص لا تظهر:
- ❌ التحقق من أن `textColor` مختلف عن لون القالب
- ✅ جرب لون متناقض مع خلفية القالب

### الموضع غير صحيح:
- ❌ تحقق من قيمة `textPosition` في قاعدة البيانات
- ✅ استخدم أحد القيم الـ 9 المدعومة فقط

### الخط لا يتغير:
- ❌ `textFont` يُخزّن الاسم فقط
- ✅ الخط يُطبّق من `index.css` تلقائياً

## تحديثات مستقبلية مقترحة

- [ ] إضافة ظلال وتأثيرات على النصوص (Text Shadow)
- [ ] دعم شفافية النصوص (Opacity)
- [ ] إضافة خطوط إضافية (Fonts)
- [ ] معاينة مباشرة للنص على القالب
- [ ] حفظ إعدادات مفضلة
